# O3 唤醒任务表优化（性能专题 · 追踪入口）

> 本目录存放"**唤醒路径（`SendNotify` / 事件调度 / 锁窗口）优化**"性能专题的**实测研究**与**原型补丁**，是本仓库**性能专题的实测数据来源与后续追踪入口**（相应的"性能条目"已在 `问题清单` 中删档，**数据保留在本目录**）。

## 当前状态（追踪）

| 项                       | 状态                                                                                                                  |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| **本目录状态**           | **暂不做（2026-09-14 用户决策：整体不划算）** —— 方案已评估、原型已验证，但**未纳入框架**；保持"待条件变化时再评估" |
| **完整 O3 结论**         | **不建议实施**：p≈2%（中断命中锁窗口比例）下**净亏 1.7% CPU**                                                        |
| 推荐替代（唤醒路径优化） | **"零回归登记表"**：只把延迟路径换成"单槽/小环登记"（保留原直接路径与锁），**尚未实施**                               |
| 与时间表插入优化的关系   | 时间表插入 O(M) 且在锁内（实测 **335 周期/次**）是本专题最大项，**优先级高于唤醒路径优化**                            |
| 重新评估条件             | 仅当 `p ≥ 17%`（延时任务显著更多/锁窗口更长）或需要"**中断侧挂起/恢复**"时，再评估完整 O3                            |
| 关联章节                 | 正文 §6（实测数据表）、§7 结论第 4 条                                                                               |

## 关键实测数据（摘要；完整数据见正文 §10）

| 测点                          | 值                                   |
| ----------------------------- | ------------------------------------ |
| 时间表插入（20 节点，锁内）   | **335 周期/次**                      |
| 事件补偿扫描（21 节点，持锁） | **315 周期**                         |
| 搬表 `MoveToReadyList`        | **42 周期**                          |
| `SendNotify`（目标在等待）    | **104 周期**                         |
| 每轮门控（现状 / 登记式）     | **13 / 9 周期**                      |
| 代码体积（示例全量）          | 现状 **2256B** / 变体 **2188B**      |
| 通知机制整体占用              | 4×2 Mbps 下 ≈ **4.1% CPU**（现状） |

## 文件

| 文件                               | 说明                                                           |
| ---------------------------------- | -------------------------------------------------------------- |
| [修正方案.md](./修正方案.md)       | 研究正文：方案对比、实测方法、逐项数据、结论与**三次更正过程** |
| [原型改动.patch](./原型改动.patch) | 完整 O3 的原型补丁（**已否决，仅作参考，勿直接套用**）         |

## 后续行动

> **2026-09-14 决策：本专题暂不实施 —— "整体不划算"。**
> 理由：时间表插入优化的 ≈10% CPU 收益依赖"任务数/锁窗口分布"前提，与"改 TCB 布局 + 全量回归 + 锁语义变化"的代价相比不划算；完整 O3 早已否决。下面 1~3 条**保留为将来重开时的行动清单**（数据与补丁均有效）。

1. **时间表插入优化**（最大收益项）：分桶 / 时间轮 —— 实施前先用本目录的测量流程建立**插入周期基线**（目标 ≤100 周期）；
2. **唤醒路径优化**：零回归登记表（每次省 ~272 周期；p≈2% 下收益有限但零回归）；
3. **完整 O3**：保持否决；仅当 `p ≥ 17%` 或需要"中断侧挂起/恢复"时重开评估；
4. 测量与复现流程：`Docs/Skill/MDK_Sim_Agent_Skill.md`（无界面仿真）。
